# GOAI Agent 工作协议

## 项目定位

本项目用于 GOAI Agent Infra 赛道的方案、Demo、评估证据与参赛材料制作。当前业务定位是“面向旅行供应链异常的多 Agent 智能客服自主闭环”。

## 工作优先级

1. 先跑通一条合成案例的端到端最小闭环；
2. 再补异常分支、Human Review、Trace 与评估；
3. 最后制作 PPT、演示素材和提交包；
4. 不因研究推荐技术栈而延误可验证闭环。

## 数据与安全边界

- 禁止复制或使用 PKFARE 内部代码、真实邮件、真实订单、客户信息、商业秘密和未公开规则。
- 允许参考历史项目的方法、抽象流程、Agent 分工、Schema 设计与评估思路，但新资产必须在本项目中基于合成数据重新制作。
- 高风险动作必须包含审批、状态前置校验、幂等标识、审计记录和失败处理。
- 未经用户明确授权，不执行外部报名、作品提交、群聊加入或外部消息发送。

## 目录写入规则

- 正式 Spec 统一写入根目录 `specs/`，它是产品行为、验收边界和后续实现的唯一事实源。
- 背景与业务判断写入 `01-项目背景/`。
- 官方材料和技术调研写入 `02-赛事调研/`。
- 尚未收敛的需求分析、流程推导与取舍记录写入 `03-需求定义/`。
- Agent、Skill、架构和治理方案写入 `04-方案设计/`。
- 合成输入、Schema 和知识卡写入 `05-数据资产/`。
- 评估设计、测试集和运行结论写入 `06-评估验证/`。
- PPT 源文件、截图、Trace 和演示素材写入 `07-参赛材料/`。
- 最终可提交版本写入 `08-交付输出/`。
- 代码、配置、Skills、Mock 服务和运行产物写入 `workspace/`。

## Spec 创建与维护规则

本项目以 [GitHub Spec Kit](https://github.com/github/spec-kit) 的 Spec-Driven Development 结构为创建标准。

### 目录与命名

- 每个可独立验收的功能使用一个 `specs/NNN-short-name/` 目录。
- `NNN` 使用三位递增编号，例如 `001`、`002`。
- `short-name` 使用简短的英文 kebab-case，描述交付能力，不写临时任务名或技术名。
- 每个功能目录必须有 `spec.md`；`specs/README.md` 维护全部 Spec 的索引和状态。

### 产物顺序

1. `spec.md`：定义为什么做、为谁做、用户场景、功能需求、边界和可验收结果；不在这一阶段预设技术实现。
2. `plan.md`：在 `spec.md` 确认后，定义技术实现、项目结构和复杂度取舍。
3. `research.md`、`data-model.md`、`contracts/`、`quickstart.md`：只在实现计划确实需要时创建。
4. `tasks.md`：从已确认的 Spec 和 Plan 拆解出可执行、有依赖关系的实现任务。
5. 实现与验证：按 `tasks.md` 执行，并回到 `spec.md` 逐项校验是否满足验收标准。

### 事实源与变更

- `spec.md` 是“要构建什么”的事实源；`plan.md`、`tasks.md` 和实现产物都从它派生。
- 需求或验收口径变更时，先修改 `spec.md`，再同步 Plan、Tasks、测试和演示材料。
- 调研草稿、头脑风暴和未确认方案不写入 `specs/`；它们保留在 `01-项目背景/` 至 `04-方案设计/` 对应目录。
- 代码、AgentTeams 配置、Skill 实现、Mock 服务和运行日志不放入 `specs/`。
- 创建 Plan 或开始实现前，必须先获得用户对 `spec.md` 的确认。

## 真实性口径

材料必须明确区分：

- `已实现`：在 AgentTeams 中真实运行并有证据；
- `模拟执行`：调用本地 Mock API 或合成数据库完成；
- `方案设计`：已设计但本轮未实现；
- `后续规划`：晋级或生产化阶段再建设。

## 长期项目 Agent 协作

所有建设 GOAI 项目的长期 Codex Agent，接收正式任务前必须完整读取 [`04-方案设计/04-安全与治理/多Agent协作表.md`](04-方案设计/04-安全与治理/多Agent协作表.md)。该表是身份、授权、任务交接、向上汇报和验收规则的详细事实源。

项目 Agent 与参赛产品内部 Agent 是两类身份，不得混淆：

- Planning Agent 维护 Spec、后续 Plan 与 Tasks、优先级、架构决策、验收、风险和跨任务协调，不包揽日常实现；
- 执行 Agent 按已确认的 Spec、Plan 与 Tasks 实现 AgentTeams、Skills、MCP/Mock、测试、运行证据和参赛素材，不得自行改变产品边界、角色职责、架构或验收口径。

项目按 `Spec → 用户确认 → Plan → 用户确认 → Tasks → 实施 → 证据 → 验收` 推进。功能 `001` 的 Spec 与 Plan 已于 2026-08-14 确认，`tasks.md` 已生成；执行 Agent 只能按 Tasks 实施，范围、架构或验收口径变化必须先交回 Planning Agent。

项目 Agent 即时向上汇报只使用 `请决策`、`请协调`、`请升级`、`请验收` 四种类型，并写明触发事实、证据位置和主控需要采取的动作。普通进展不即时汇报。

未经用户逐次明确授权，任何项目 Agent 不得报名、提交作品、加入群聊或发送外部消息。
