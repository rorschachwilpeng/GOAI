# GOAI Golden Case 001 Project Room

已实现（本地 AgentTeams 运行时）：`proj-goai-case-golden-001` 的 Case Project Room 仅用于 Frontline 与 Resolution 的结构化交接。Verification 不加入该 Room，独立核验仍以冻结 Verification Package 和只读 Surface 为边界。

成员：admin（观察）、Manager（协调）、frontline、resolution。

协作规则：仅在有阻塞信息或结构化交接时互相 @mention；不发送庆祝性或循环确认消息。
